---
import MainLayout from "../layouts/MainLayout.astro";

const projects = [
  {
    title: "PipeSlayer",
    slug: "pipeslayer",
    image: "/images/pipeslayer2.png",
    hoverImage: "/images/pipeslayer-fullview.png",
    href: "/dewalt-pipeslayer",
  },
  {
    title: "Vielle",
    slug: "vielle",
    image: "/images/vielle2.png",
    hoverImage: "/images/vielle-fullview.png",
    href: "/vielle",
  },
  {
    title: "Zumi",
    slug: "zumi",
    image: "/images/zumi2.png",
    hoverImage: "/images/zumi-fullview.png",
    href: "/zumi",
  },
];
---

<MainLayout title="Lillian Wray">
  <style>
    .projects {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 50px;
    }

    .project {
      position: relative;
      display: block;
      color: inherit;
      text-decoration: none;
    }

    .project:first-child {
      grid-column: 1 / -1;
    }

    .project-image {
      position: relative;
      width: 100%;
      height: 520px;
      overflow: hidden;
      border-radius: 8px;
      background: var(--bg);
    }

    .project:first-child .project-image {
      height: 560px;
    }

    .project-pipeslayer .project-image {
      background: rgb(242, 242, 242);
    }

    .project-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    .cover-image,
    .hover-image {
      transform: scale(1);
      transition:
        opacity 760ms ease-in-out,
        transform 760ms cubic-bezier(0.22, 1, 0.36, 1);
    }

    .cover-image {
      opacity: 1;
    }

    .hover-image {
      position: absolute;
      inset: 0;
      opacity: 0;
      transform: scale(1);
    }

    .project h2 {
      position: absolute;
      left: 32px;
      bottom: 28px;
      margin: 0;
      color: #282828;
      font-family: "proxima-nova", Arial, sans-serif;
      font-size: 2.9rem;
      font-weight: 500;
      letter-spacing: 0;
      opacity: 0;
      transform: translateY(6px);
      transition:
        transform 180ms cubic-bezier(0.22, 1, 0.36, 1),
        opacity 180ms ease-in-out;
      z-index: 1;
    }

    .project-vielle h2 {
      color: var(--bg);
    }

    .project-pipeslayer .hover-image {
      object-fit: contain;
      object-position: right center;
    }

    .project-vielle .hover-image {
      object-position: left center;
      transform: scaleX(-1) scale(1);
    }

    .project-zumi .hover-image {
      object-position: 65% center;
    }

    .project:hover h2,
    .project:focus-visible h2 {
      opacity: 1;
      transform: translateY(0);
      transition:
        transform 700ms cubic-bezier(0.22, 1, 0.36, 1),
        opacity 700ms ease-in-out;
    }

    .project:hover .cover-image,
    .project:focus-visible .cover-image {
      opacity: 0;
      transform: scale(1.03);
    }

    .project:hover .hover-image,
    .project:focus-visible .hover-image {
      opacity: 1;
      transform: scale(1.05);
    }

    .project-vielle:hover .hover-image,
    .project-vielle:focus-visible .hover-image {
      transform: scaleX(-1) scale(1.05);
    }

    @media (max-width: 900px) {
      .projects {
        grid-template-columns: 1fr;
        gap: 28px;
      }

      .project:first-child {
        grid-column: auto;
      }

      .project:first-child .project-image,
      .project-image {
        height: 240px;
      }

      .hover-image {
        display: none;
      }

      .project-image img,
      .project:hover .cover-image,
      .project:focus-visible .cover-image {
        opacity: 1;
        transform: none;
      }

      .project h2 {
        position: static;
        margin: 12px 0 0;
        font-size: 1rem;
        font-weight: 600;
        color: #282828;
        opacity: 1;
        transform: none;
        transition: none;
      }
    }
  </style>

  <section class="projects">
    {
      projects.map((project) => (
        <a class={`project project-${project.slug}`} href={project.href}>
          <div class="project-image">
            <img class="cover-image" src={project.image} alt={project.title} />
            <img class="hover-image" src={project.hoverImage} alt="" />
          </div>
          <h2>{project.title}</h2>
        </a>
      ))
    }
  </section>
</MainLayout>
